# URL ของ backend API
# local dev (docker compose): http://localhost:8000
# production: ตั้งเป็น environment variable นี้บน hosting platform (Vercel/Netlify/Render ฯลฯ) ตอน build
NUXT_PUBLIC_API_URL=http://localhost:8000
